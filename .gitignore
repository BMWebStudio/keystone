node_modules
.next
dist
.env
.env.local
.env.*.local
.DS_Store
*.log
coverage
.turbo
*.tsbuildinfo

# Submission export tooling — local only on main; export script copies README into submission repo
scripts/export-submission.mjs
scripts/submission-README.md

# Local AI / editor tooling — do not commit
.agents/
.claude/
.cursor/
skills-lock.json
